# ClawMobile App currently uses in-app agent profile configuration.
# No mandatory environment variables are required for local development.
#
# Optional convention for future use:
# EXPO_PUBLIC_DEFAULT_AGENT_WS_URL=ws://127.0.0.1:8787/ws
# EXPO_PUBLIC_DEFAULT_CLIENT_ID=dev-mobile
